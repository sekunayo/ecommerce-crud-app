import { css } from "@emotion/css"

const cartView = css({
    marginTop: "50px",
    display: "flex",
    justifyContent: "flex-end",
    width: "100%"
})

const cartViewItemList = css({
    border: "1px solid #b1a7a6"
})

export const styles = {
    cartView,
    cartViewItemList
}