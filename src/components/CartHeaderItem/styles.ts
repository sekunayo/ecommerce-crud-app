import { css } from "@emotion/css";

const cartHeaderItemImage = css({
    height: "70px",
    width: "70px"
})

const cartHeaderItem = css({
    display: "flex",
    gap: "10px",
    alignItems: "center"
})

export const styles = {
    cartHeaderItemImage,
    cartHeaderItem
}

